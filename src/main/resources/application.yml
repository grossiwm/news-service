newsData:
  rootUrl: https://newsdata.io/api/1
  apiKey:
management:
  security:
    enabled: false
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
springdoc:
  api-docs:
    path: /api-docs